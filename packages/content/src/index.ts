/**
 * @metrivio/content — Stage 1 scaffold only.
 *
 * This package will hold the Research → Idea → Draft → Quality Check →
 * Human Approval → Schedule → Publish → Analyze pipeline (ARCHITECTURE.md
 * §4), built on top of the vendored X-Manager instance at
 * ./vendor/x-manager (see VENDOR.md in that directory for the pinned
 * commit), across BUILD_PLAN.md Stages 10–12.
 *
 * Nothing functional lives here yet — this is scaffolding only, per Stage 1
 * scope.
 */
export const CONTENT_PACKAGE_STAGE = 'scaffold-only' as const;
